// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Defines the app install states that are returned by the InstallState property of the AppInstallStatus class.
public struct WindowsApplicationModelStorePreviewInstallControl_AppInstallState: RawRepresentable, Hashable, Codable {
    public var rawValue: Swift.Int32

    public init(rawValue: Swift.Int32 = 0) {
        self.rawValue = rawValue
    }

    /// The app install is pending.
    public static let pending = Self()

    /// The app install is starting.
    public static let starting = Self(rawValue: 1)

    /// The license is being acquired for the app.
    public static let acquiringLicense = Self(rawValue: 2)

    /// The app is being downloaded.
    public static let downloading = Self(rawValue: 3)

    /// The app data is being restored.
    public static let restoringData = Self(rawValue: 4)

    /// The app is in the process of being installed.
    public static let installing = Self(rawValue: 5)

    /// The app install is complete.
    public static let completed = Self(rawValue: 6)

    /// The app install is canceled.
    public static let canceled = Self(rawValue: 7)

    /// The app install was paused.
    public static let paused = Self(rawValue: 8)

    /// The app install encountered an error.
    public static let error = Self(rawValue: 9)

    /// The app install was paused because the battery is low.
    public static let pausedLowBattery = Self(rawValue: 10)

    /// The app install was paused because the app package was large enough to trigger a prompt for the user to download the app over Wi-Fi.
    public static let pausedWiFiRecommended = Self(rawValue: 11)

    /// The app install was paused because the app package was large enough to require the user to download the app over Wi-Fi.
    public static let pausedWiFiRequired = Self(rawValue: 12)

    /// The app is ready to download.
    public static let readyToDownload = Self(rawValue: 13)
}