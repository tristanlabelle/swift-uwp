// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Represents the capabilities common to all types of stations for point-of-service printers.
public protocol WindowsDevicesPointOfService_ICommonPosPrintStationCapabilitiesProtocol: IInspectableProtocol {
    /// Gets information about the sensors that the printer station has available to report the status of the printer station.
    /// - Returns: A combination of enumeration values that indicate the sensors that the printer station has available to report the status of the printer station.
    var cartridgeSensors: WindowsDevicesPointOfService_PosPrinterCartridgeSensors { get throws }

    /// Gets the color cartridges that the printer station can use to print in color.
    /// - Returns: A combination of enumeration values that indicates which color cartridges the printer station can use to print in color.
    var colorCartridgeCapabilities: WindowsDevicesPointOfService_PosPrinterColorCapabilities { get throws }

    /// Gets whether the printer station can print bold characters.
    /// - Returns: True if the printer station can print bold characters; otherwise false.
    var isBoldSupported: Swift.Bool { get throws }

    /// Gets whether the printer station can print characters that are both double-high and double-wide.
    /// - Returns: True if the printer station can print characters that are both double-high and double-wide; otherwise false.
    var isDoubleHighDoubleWidePrintSupported: Swift.Bool { get throws }

    /// Gets whether the printer station can print double-high characters.
    /// - Returns: True if the printer station can print double-high characters; otherwise false.
    var isDoubleHighPrintSupported: Swift.Bool { get throws }

    /// Gets whether the printer station can print double-wide characters.
    /// - Returns: True if the printer station can print double-wide characters; otherwise false.
    var isDoubleWidePrintSupported: Swift.Bool { get throws }

    /// Gets whether the printer station can print a dark color plus an alternate color.
    /// - Returns: True if the printer station can print a dark color plus an alternate color; otherwise false.
    var isDualColorSupported: Swift.Bool { get throws }

    /// Gets whether the printer station can print italic characters.
    /// - Returns: True if the printer station can print italic characters; otherwise false.
    var isItalicSupported: Swift.Bool { get throws }

    /// Gets whether the printer station has an out-of-paper sensor.
    /// - Returns: True if the printer station has an out-of-paper sensor; otherwise false.
    var isPaperEmptySensorSupported: Swift.Bool { get throws }

    /// Gets whether the printer station has a low-paper sensor.
    /// - Returns: True if the printer station has a low-paper sensor; otherwise false.
    var isPaperNearEndSensorSupported: Swift.Bool { get throws }

    /// Gets whether a printer station is present.
    /// - Returns: True if a printer station is present; otherwise false.
    var isPrinterPresent: Swift.Bool { get throws }

    /// Gets whether the printer station can underline characters.
    /// - Returns: True if the printer station can underline characters; otherwise false.
    var isUnderlineSupported: Swift.Bool { get throws }

    /// Gets a collection of the line widths in characters per line that the printer station supports.
    /// - Returns: A collection of the line widths in characters per line that the printer station supports.
    var supportedCharactersPerLine: WindowsFoundationCollections_IVectorView<Swift.UInt32> { get throws }
}

/// Represents the capabilities common to all types of stations for point-of-service printers.
public typealias WindowsDevicesPointOfService_ICommonPosPrintStationCapabilities = any WindowsDevicesPointOfService_ICommonPosPrintStationCapabilitiesProtocol