// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// The kinds of phone numbers returned by PhoneNumberInfo.PredictNumberKind.
public struct WindowsGlobalizationPhoneNumberFormatting_PredictedPhoneNumberKind: RawRepresentable, Hashable, Codable {
    public var rawValue: Swift.Int32

    public init(rawValue: Swift.Int32 = 0) {
        self.rawValue = rawValue
    }

    /// A fixed telephone line.
    public static let fixedLine = Self()

    /// A mobile line.
    public static let mobile = Self(rawValue: 1)

    /// Either a fixed line or a mobile line.
    public static let fixedLineOrMobile = Self(rawValue: 2)

    /// A toll-free number.
    public static let tollFree = Self(rawValue: 3)

    /// A number that charges a premium rate to connect.
    public static let premiumRate = Self(rawValue: 4)

    /// A shared-cost service (caller pays part of the cost, recipient pays the rest.)
    public static let sharedCost = Self(rawValue: 5)

    /// A VOIP (Voice over IP) line.
    public static let voip = Self(rawValue: 6)

    /// A number that reaches a private individual.
    public static let personalNumber = Self(rawValue: 7)

    /// A number that reaches a pager.
    public static let pager = Self(rawValue: 8)

    /// A category that includes national services and corporate, institutional, or private network access numbers.
    public static let universalAccountNumber = Self(rawValue: 9)

    /// A number that reaches a voicemail service.
    public static let voicemail = Self(rawValue: 10)

    /// A number that is none of the other kinds in this enumeration, or whose precise kind is not known.
    public static let unknown = Self(rawValue: 11)
}