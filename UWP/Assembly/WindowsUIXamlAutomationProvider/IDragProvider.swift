// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Enables a Microsoft UI Automation element to describe itself as an element that can be dragged as part of a drag-and-drop operation. Implement this interface in order to support the capabilities that an automation client requests with a GetPattern call and PatternInterface.Drag.
public protocol WindowsUIXamlAutomationProvider_IDragProviderProtocol: IInspectableProtocol {
    /// Gets a string that indicates what will happen when the item is dropped.
    /// - Returns: A string that indicates what will happen when the item is dropped.
    var dropEffect: Swift.String { get throws }

    /// Gets an array of strings that enumerates possible drop effects when this item is dropped.
    /// - Returns: An array of strings that enumerates possible drop effects when this item is dropped.
    var dropEffects: [Swift.String] { get throws }

    /// Gets a value indicating whether an item is currently being dragged.
    /// - Returns: **true** if the item is being dragged. Otherwise, **false**.
    var isGrabbed: Swift.Bool { get throws }

    /// Gets an array of UI Automation elements that are being dragged as part of this drag operation.
    /// - Returns: An array of UI Automation elements that are being dragged. Null if this item is an individual item being dragged. Used to enable providers that support dragging multiple items at a time to create an intermediary IDragProvider that encapsulates all of the items being dragged.
    func getGrabbedItems() throws -> [WindowsUIXamlAutomationProvider_IRawElementProviderSimple?]
}

/// Enables a Microsoft UI Automation element to describe itself as an element that can be dragged as part of a drag-and-drop operation. Implement this interface in order to support the capabilities that an automation client requests with a GetPattern call and PatternInterface.Drag.
public typealias WindowsUIXamlAutomationProvider_IDragProvider = any WindowsUIXamlAutomationProvider_IDragProviderProtocol