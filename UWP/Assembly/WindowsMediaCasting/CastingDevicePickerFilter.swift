// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Represents the filter used to determine which devices to show in a casting device picker UI. The filter parameters are OR-ed together to build the resulting filter. In other words, if SupportsAudio and SupportsVideo are both true, the picker will display Audio-only devices, video-only devices, and audio/video devices.
public final class WindowsMediaCasting_CastingDevicePickerFilter: WindowsRuntime.WinRTImport<WindowsMediaCasting_CastingDevicePickerFilterProjection> {
    // MARK: Windows.Media.Casting.ICastingDevicePickerFilter members

    /// Gets the casting sources supported.
    /// - Returns: The list of supported casting sources.
    public var supportedCastingSources: WindowsFoundationCollections_IVector<WindowsMediaCasting_CastingSource?> {
        get throws {
            try COM.NullResult.unwrap(_interop.get_SupportedCastingSources())
        }
    }

    /// Gets and sets whether the devices in the device picker should support audio playback.
    /// - Returns: If true, the picker will show audio-capable casting devices.
    public var supportsAudio: Swift.Bool {
        get throws {
            try _interop.get_SupportsAudio()
        }
    }

    /// Gets and sets whether the devices in the device picker should support audio playback.
    /// - Returns: If true, the picker will show audio-capable casting devices.
    public func supportsAudio(_ value: Swift.Bool) throws {
        try _interop.put_SupportsAudio(value)
    }

    /// Gets and sets whether the devices in the device picker should support rending still images.
    /// - Returns: If true, the picker will show casting devices capable of rendering still images.
    public var supportsPictures: Swift.Bool {
        get throws {
            try _interop.get_SupportsPictures()
        }
    }

    /// Gets and sets whether the devices in the device picker should support rending still images.
    /// - Returns: If true, the picker will show casting devices capable of rendering still images.
    public func supportsPictures(_ value: Swift.Bool) throws {
        try _interop.put_SupportsPictures(value)
    }

    /// Gets and sets whether the devices in the device picker should support video playback.
    /// - Returns: If true, the picker will show video-capable casting devices.
    public var supportsVideo: Swift.Bool {
        get throws {
            try _interop.get_SupportsVideo()
        }
    }

    /// Gets and sets whether the devices in the device picker should support video playback.
    /// - Returns: If true, the picker will show video-capable casting devices.
    public func supportsVideo(_ value: Swift.Bool) throws {
        try _interop.put_SupportsVideo(value)
    }

    // MARK: Implementation details
}