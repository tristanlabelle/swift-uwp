// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Represents a set of download statistics associated with the AdaptiveMediaSource.DownloadFailed and AdaptiveMediaSource.DownloadCompleted events.
public final class WindowsMediaStreamingAdaptive_AdaptiveMediaSourceDownloadStatistics: WindowsRuntime.WinRTImport<WindowsMediaStreamingAdaptive_AdaptiveMediaSourceDownloadStatisticsProjection> {
    // MARK: Windows.Media.Streaming.Adaptive.IAdaptiveMediaSourceDownloadStatistics members

    /// Gets the count of bytes that have been received at the time of the event.
    /// - Returns: The count of bytes that have been received at the time of the event.
    public var contentBytesReceivedCount: Swift.UInt64 {
        get throws {
            try _interop.get_ContentBytesReceivedCount()
        }
    }

    /// Gets the time window between when the system makes the asyncronous http request for the content and when the first block of data has been read from the http stream.
    /// - Returns: The time window between when the system makes the asyncronous http request for the content and when the first block of data has been read from the http stream.
    public var timeToFirstByteReceived: WindowsFoundation_IReference<WindowsFoundation_TimeSpan>? {
        get throws {
            try _interop.get_TimeToFirstByteReceived()
        }
    }

    /// Gets the time window between when the system makes the asyncronous http request for the content and when the operation completes, when the http headers have been received.
    /// - Returns: The time window between when the system makes the asyncronous http request for the content and when the operation completes, when the http headers have been received.
    public var timeToHeadersReceived: WindowsFoundation_IReference<WindowsFoundation_TimeSpan>? {
        get throws {
            try _interop.get_TimeToHeadersReceived()
        }
    }

    /// Gets the time window between when the system makes the asyncronous http request for the content and when the last block of data has been read from the http stream.
    /// - Returns: The time window between when the system makes the asyncronous http request for the content and when the last block of data has been read from the http stream.
    public var timeToLastByteReceived: WindowsFoundation_IReference<WindowsFoundation_TimeSpan>? {
        get throws {
            try _interop.get_TimeToLastByteReceived()
        }
    }

    // MARK: Implementation details
}