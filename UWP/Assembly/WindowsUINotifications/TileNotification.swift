// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Defines an update to a tile, including its visuals, identification tag, and expiration time.
public final class WindowsUINotifications_TileNotification: WindowsRuntime.WinRTImport<WindowsUINotifications_TileNotificationProjection> {
    // MARK: Windows.UI.Notifications.ITileNotificationFactory members

    public convenience init(_ content: WindowsDataXmlDom_XmlDocument?) throws {
        self.init(_transferringRef: try COM.NullResult.unwrap(Self._itileNotificationFactory.createTileNotification(content)))
    }

    // MARK: Windows.UI.Notifications.ITileNotification members

    /// Gets the XML description of the notification content, which you can then manipulate to alter the notification.
    /// - Returns: The object that contains the notification content.
    public var content: WindowsDataXmlDom_XmlDocument {
        get throws {
            try COM.NullResult.unwrap(_interop.get_Content())
        }
    }

    /// Gets or sets the time that Windows will remove the notification from the tile. By default, a tile update does not expire. It is a best practice to explicitly set an expiration time to avoid stale content.
    /// - Returns: The date and time that the notification should be removed.
    public var expirationTime: WindowsFoundation_IReference<WindowsFoundation_DateTime>? {
        get throws {
            try _interop.get_ExpirationTime()
        }
    }

    /// Gets or sets the time that Windows will remove the notification from the tile. By default, a tile update does not expire. It is a best practice to explicitly set an expiration time to avoid stale content.
    /// - Returns: The date and time that the notification should be removed.
    public func expirationTime(_ value: WindowsFoundation_IReference<WindowsFoundation_DateTime>?) throws {
        try _interop.put_ExpirationTime(value)
    }

    /// Gets or sets a string that Windows can use to prevent duplicate notification content from appearing in the queue.
    /// - Returns: A string of 16 characters or less (plus a terminating null character) that identifies the notification in the stack. While there is no set form to the string content, we recommend that it should relate to the content of the notification.
    public var tag: Swift.String {
        get throws {
            try _interop.get_Tag()
        }
    }

    /// Gets or sets a string that Windows can use to prevent duplicate notification content from appearing in the queue.
    /// - Returns: A string of 16 characters or less (plus a terminating null character) that identifies the notification in the stack. While there is no set form to the string content, we recommend that it should relate to the content of the notification.
    public func tag(_ value: Swift.String) throws {
        try _interop.put_Tag(value)
    }

    // MARK: Implementation details

    private static var _itileNotificationFactory_storage: COM.COMInterop<CWinRT.SWRT_WindowsUINotifications_ITileNotificationFactory>? = nil

    internal static var _itileNotificationFactory: COM.COMInterop<CWinRT.SWRT_WindowsUINotifications_ITileNotificationFactory> {
        get throws {
            try _itileNotificationFactory_storage.lazyInit {
                try WindowsRuntime.getActivationFactoryPointer(activatableId: "Windows.UI.Notifications.TileNotification", id: CWinRT.SWRT_WindowsUINotifications_ITileNotificationFactory.iid)
            }
        }
    }
}