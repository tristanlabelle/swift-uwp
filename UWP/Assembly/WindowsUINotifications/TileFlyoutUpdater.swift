// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Used to send or clear a tile flyout (mix view) notification. This class also provides methods to start and stop [periodic](https://docs.microsoft.com/previous-versions/windows/apps/jj150587(v=win.10)) tile flyout notifications.
public final class WindowsUINotifications_TileFlyoutUpdater: WindowsRuntime.WinRTImport<WindowsUINotifications_TileFlyoutUpdaterProjection> {
    // MARK: Windows.UI.Notifications.ITileFlyoutUpdater members

    /// Gets a value that specifies whether a tile's flyout can be updated through notifications.
    /// - Returns: This value always returns **Enabled**. Mix view cannot currently be disabled.
    public var setting: WindowsUINotifications_NotificationSetting {
        get throws {
            try _interop.get_Setting()
        }
    }

    /// Sends a new tile flyout (mix view) notification to the primary or secondary tile associated with the updater.
    /// - Parameter notification: The notification object that supplies the new content for the tile's flyout.
    public func update(_ notification: WindowsUINotifications_TileFlyoutNotification?) throws {
        try _interop.update(notification)
    }

    /// Removes the current tile flyout (mix view) notification. A tile flyout notification does not have a default expiration time, so unless one is set explicitly through TileFlyoutNotification.ExpirationTime, this method is the only way to remove the flyout.
    public func clear() throws {
        try _interop.clear()
    }

    /// Launches a series of [periodic](https://docs.microsoft.com/previous-versions/windows/apps/jj150587(v=win.10)) updates for a tile flyout (mix view) notification, beginning with an immediate update when the method is called. Update content is retrieved from a specified Uniform Resource Identifier (URI).
    /// - Parameter tileFlyoutContent: The Uniform Resource Identifier (URI) from which the XML content of the tile flyout update is retrieved.
    /// - Parameter requestedInterval: The frequency with which the Uniform Resource Identifier (URI) is polled for new content, following the initial update.
    public func startPeriodicUpdate(_ tileFlyoutContent: WindowsFoundation_Uri?, _ requestedInterval: WindowsUINotifications_PeriodicUpdateRecurrence) throws {
        try _interop.startPeriodicUpdate(tileFlyoutContent, requestedInterval)
    }

    /// Launches a series of [periodic](https://docs.microsoft.com/previous-versions/windows/apps/jj150587(v=win.10)) updates for a tile flyout (mix view) notification, beginning at a specified time. Update content is retrieved from a specified Uniform Resource Identifier (URI).
    /// - Parameter tileFlyoutContent: The Uniform Resource Identifier (URI) from which the XML content of the tile flyout update is retrieved.
    /// - Parameter startTime: The date and time at which the Uniform Resource Identifier (URI) should first be polled for new content.
    /// - Parameter requestedInterval: The frequency with which the Uniform Resource Identifier (URI) is polled for new content, following the initial update at *startTime*.
    public func startPeriodicUpdate(_ tileFlyoutContent: WindowsFoundation_Uri?, _ startTime: WindowsFoundation_DateTime, _ requestedInterval: WindowsUINotifications_PeriodicUpdateRecurrence) throws {
        try _interop.startPeriodicUpdateAtTime(tileFlyoutContent, startTime, requestedInterval)
    }

    /// Cancels the current series of [periodic](https://docs.microsoft.com/previous-versions/windows/apps/jj150587(v=win.10)) tile flyout (mix view) notification updates for the primary or secondary tile associated with the updater.
    public func stopPeriodicUpdate() throws {
        try _interop.stopPeriodicUpdate()
    }

    // MARK: Implementation details
}