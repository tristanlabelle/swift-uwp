// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Updates a badge overlay on the specific tile that the updater is bound to.
public final class WindowsUINotifications_BadgeUpdater: WindowsRuntime.WinRTImport<WindowsUINotifications_BadgeUpdaterProjection> {
    // MARK: Windows.UI.Notifications.IBadgeUpdater members

    /// Applies a change to the badge's glyph or number.
    /// - Parameter notification: The object that supplies the new XML definition for the badge.
    public func update(_ notification: WindowsUINotifications_BadgeNotification?) throws {
        try _interop.update(notification)
    }

    /// Removes the badge from the tile that the updater is bound to.
    public func clear() throws {
        try _interop.clear()
    }

    /// Begins a series of timed updates for the badge from a web resource that the updater is bound to, beginning immediately. Note that only web resources (http/https) are allowed in a periodic update.
    /// - Parameter badgeContent: The Uniform Resource Identifier (URI) from which the XML content of the badge update will be retrieved.
    /// - Parameter requestedInterval: The frequency with which the Uniform Resource Identifier (URI) is polled for new badge content.
    public func startPeriodicUpdate(_ badgeContent: WindowsFoundation_Uri?, _ requestedInterval: WindowsUINotifications_PeriodicUpdateRecurrence) throws {
        try _interop.startPeriodicUpdate(badgeContent, requestedInterval)
    }

    /// Begins a series of timed updates for the badge from a web resource that the updater is bound to. Updates begin at a specified time. Note that only web resources (http/https) are allowed in a periodic update.
    /// - Parameter badgeContent: The Uniform Resource Identifier (URI) from which the XML content of the badge update will be retrieved.
    /// - Parameter startTime: The time at which the Uniform Resource Identifier (URI) should first be polled for new badge content.
    /// - Parameter requestedInterval: The frequency with which the Uniform Resource Identifier (URI) is polled for new badge content, following the initial update at *startTime*.
    public func startPeriodicUpdate(_ badgeContent: WindowsFoundation_Uri?, _ startTime: WindowsFoundation_DateTime, _ requestedInterval: WindowsUINotifications_PeriodicUpdateRecurrence) throws {
        try _interop.startPeriodicUpdateAtTime(badgeContent, startTime, requestedInterval)
    }

    /// Cancels the current series of timed updates for the badge that the updater is bound to.
    public func stopPeriodicUpdate() throws {
        try _interop.stopPeriodicUpdate()
    }

    // MARK: Implementation details
}