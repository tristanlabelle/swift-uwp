// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Contains methods and properties for customizing the drag and drop UI.
public final class WindowsApplicationModelDataTransferDragDropCore_CoreDragUIOverride: WindowsRuntime.WinRTImport<WindowsApplicationModelDataTransferDragDropCore_CoreDragUIOverrideProjection> {
    // MARK: Windows.ApplicationModel.DataTransfer.DragDrop.Core.ICoreDragUIOverride members

    /// Gets or sets the caption for the drag UI.
    /// - Returns: The caption for the drag UI.
    public var caption: Swift.String {
        get throws {
            try _interop.get_Caption()
        }
    }

    /// Gets or sets the caption for the drag UI.
    /// - Returns: The caption for the drag UI.
    public func caption(_ value: Swift.String) throws {
        try _interop.put_Caption(value)
    }

    /// Gets or sets if the caption for the drag UI is visible.
    /// - Returns: True if the caption is visible; otherwise, false.
    public var isCaptionVisible: Swift.Bool {
        get throws {
            try _interop.get_IsCaptionVisible()
        }
    }

    /// Gets or sets if the caption for the drag UI is visible.
    /// - Returns: True if the caption is visible; otherwise, false.
    public func isCaptionVisible(_ value: Swift.Bool) throws {
        try _interop.put_IsCaptionVisible(value)
    }

    /// Gets or sets whether the drag content is visible.
    /// - Returns: True if the drag content is visible; otherwise, false.
    public var isContentVisible: Swift.Bool {
        get throws {
            try _interop.get_IsContentVisible()
        }
    }

    /// Gets or sets whether the drag content is visible.
    /// - Returns: True if the drag content is visible; otherwise, false.
    public func isContentVisible(_ value: Swift.Bool) throws {
        try _interop.put_IsContentVisible(value)
    }

    /// Gets or sets whether the glyph is visible.
    /// - Returns: True if the glyph is visible; otherwise, false.
    public var isGlyphVisible: Swift.Bool {
        get throws {
            try _interop.get_IsGlyphVisible()
        }
    }

    /// Gets or sets whether the glyph is visible.
    /// - Returns: True if the glyph is visible; otherwise, false.
    public func isGlyphVisible(_ value: Swift.Bool) throws {
        try _interop.put_IsGlyphVisible(value)
    }

    /// Sets custom drag UI content using a software bitmap.
    /// - Parameter softwareBitmap: The custom drag UI content.
    public func setContentFromSoftwareBitmap(_ softwareBitmap: WindowsGraphicsImaging_SoftwareBitmap?) throws {
        try _interop.setContentFromSoftwareBitmap(softwareBitmap)
    }

    /// Sets custom drag UI content using a software bitmap.
    /// - Parameter softwareBitmap: The custom UI drag content.
    /// - Parameter anchorPoint: The relative position of the drag visual from the pointer. The anchor point cannot be outside of the content. For example, if the anchor point is (50, 50) and the position of the pointer is (x, y), the top left corner for the visual will be (x - 50, y - 50).
    public func setContentFromSoftwareBitmap(_ softwareBitmap: WindowsGraphicsImaging_SoftwareBitmap?, _ anchorPoint: WindowsFoundation_Point) throws {
        try _interop.setContentFromSoftwareBitmapWithAnchorPoint(softwareBitmap, anchorPoint)
    }

    /// Clears all existing custom UI overrides.
    public func clear() throws {
        try _interop.clear()
    }

    // MARK: Implementation details
}