// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Lets an app that provides a save location specify the storageFile that represents the file to save and get a deferral so the app can respond asynchronously to a targetFileRequested event.
public final class WindowsStoragePickersProvider_TargetFileRequest: WindowsRuntime.WinRTImport<WindowsStoragePickersProvider_TargetFileRequestProjection> {
    // MARK: Windows.Storage.Pickers.Provider.ITargetFileRequest members

    /// Gets or sets the IStorageFile object that is provided to represent the file to save by the app that is providing the save location.
    /// - Returns: The object that represents the file to save. The app that called the file picker in order to save will write content to this file.
    public var targetFile: WindowsStorage_IStorageFile {
        get throws {
            try COM.NullResult.unwrap(_interop.get_TargetFile())
        }
    }

    /// Gets or sets the IStorageFile object that is provided to represent the file to save by the app that is providing the save location.
    /// - Returns: The object that represents the file to save. The app that called the file picker in order to save will write content to this file.
    public func targetFile(_ value: WindowsStorage_IStorageFile?) throws {
        try _interop.put_TargetFile(value)
    }

    /// Gets a targetFileRequestDeferral that the app providing the save location can use to respond asynchronously to a targetfilerequested event.
    /// - Returns: The targetFileRequestDeferral that the providing app can use asynchronously to indicate that it is finished responding to a targetfilerequested event.
    public func getDeferral() throws -> WindowsStoragePickersProvider_TargetFileRequestDeferral {
        try COM.NullResult.unwrap(_interop.getDeferral())
    }

    // MARK: Implementation details
}