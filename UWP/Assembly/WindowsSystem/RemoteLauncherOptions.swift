// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Specifies the options used to launch the default app for URI on a remote device.
public final class WindowsSystem_RemoteLauncherOptions: WindowsRuntime.WinRTImport<WindowsSystem_RemoteLauncherOptionsProjection> {
    // MARK: IActivationFactory members

    public convenience init() throws {
        self.init(_transferringRef: try Self._iactivationFactory.activateInstance(projection: WindowsSystem_RemoteLauncherOptionsProjection.self))
    }

    // MARK: Windows.System.IRemoteLauncherOptions members

    /// Specifies the URI of the web site to view if the app to handle the URI can't be launched on the remote device.
    /// - Returns: The URI of a website.
    public var fallbackUri: WindowsFoundation_Uri {
        get throws {
            try COM.NullResult.unwrap(_interop.get_FallbackUri())
        }
    }

    /// Specifies the URI of the web site to view if the app to handle the URI can't be launched on the remote device.
    /// - Returns: The URI of a website.
    public func fallbackUri(_ value: WindowsFoundation_Uri?) throws {
        try _interop.put_FallbackUri(value)
    }

    /// A list of package family names that should be used to launch the URI on the remote device. The first one in the list should be the preferred application to launch on the remote system (device).
    /// - Returns: The list of package family names.
    public var preferredAppIds: WindowsFoundationCollections_IVector<Swift.String> {
        get throws {
            try COM.NullResult.unwrap(_interop.get_PreferredAppIds())
        }
    }

    // MARK: Implementation details

    private static var _iactivationFactory_storage: COM.COMInterop<CWinRT.SWRT_IActivationFactory>? = nil

    internal static var _iactivationFactory: COM.COMInterop<CWinRT.SWRT_IActivationFactory> {
        get throws {
            try _iactivationFactory_storage.lazyInit {
                try WindowsRuntime.getActivationFactoryPointer(activatableId: "Windows.System.RemoteLauncherOptions", id: CWinRT.SWRT_IActivationFactory.iid)
            }
        }
    }
}