// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Represents an appointment manager for a specific user.
public final class WindowsApplicationModelAppointments_AppointmentManagerForUser: WindowsRuntime.WinRTImport<WindowsApplicationModelAppointments_AppointmentManagerForUserProjection> {
    // MARK: Windows.ApplicationModel.Appointments.IAppointmentManagerForUser members

    /// Gets the User represented by this appointment manager.
    /// - Returns: The User represented by this appointment manager.
    public var user: WindowsSystem_User {
        get throws {
            try COM.NullResult.unwrap(_interop.get_User())
        }
    }

    /// Shows the Appointments provider Add Appointment UI, to enable the user to add an appointment.
    /// - Parameter appointment: The object representing the information for the appointment to add.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Add Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Add Appointment UI displays around the button, not overlapping it.
    /// - Returns: When this method completes, it returns a **String** object that represents the appointment. This serves as an appointment identifier for future reference when updating or removing. If the appointment identifier returned is an empty string, the appointment was not added to the Appointments provider app.
    public func showAddAppointmentAsync(_ appointment: WindowsApplicationModelAppointments_Appointment?, _ selection: WindowsFoundation_Rect) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showAddAppointmentAsync(appointment, selection))
    }

    /// Shows the Appointments provider Add Appointment UI, to enable the user to add an appointment.
    /// - Parameter appointment: The object representing the information for the appointment to add.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Add Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Add Appointment UI displays around the button, not overlapping it.
    /// - Parameter preferredPlacement: The Placement that describes the preferred placement of the Add Appointment UI.
    /// - Returns: When this method completes, it returns a **String** object that represents the appointment. This serves as an appointment identifier for future reference when updating or removing. If the appointment identifier returned is an empty string, the appointment was not added to the Appointments provider app.
    public func showAddAppointmentAsync(_ appointment: WindowsApplicationModelAppointments_Appointment?, _ selection: WindowsFoundation_Rect, _ preferredPlacement: WindowsUIPopups_Placement) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showAddAppointmentWithPlacementAsync(appointment, selection, preferredPlacement))
    }

    /// Shows the Appointments provider Replace Appointment UI, to enable the user to replace an appointment.
    /// - Parameter appointmentId: The appointment identifier of the current appointment. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync or ShowReplaceAppointmentAsync call.
    /// - Parameter appointment: The object representing the information for the appointment to replace the current appointment.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Replace Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Replace Appointment UI displays around the button, not overlapping it.
    /// - Returns: When this method completes, it returns a **String** object that represents the appointment. This serves as an appointment identifier for future reference when updating or removing. If the appointment identifier returned is an empty string, the appointment was not replaced in the Appointments provider app.
    public func showReplaceAppointmentAsync(_ appointmentId: Swift.String, _ appointment: WindowsApplicationModelAppointments_Appointment?, _ selection: WindowsFoundation_Rect) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showReplaceAppointmentAsync(appointmentId, appointment, selection))
    }

    /// Shows the Appointments provider Replace Appointment UI, to enable the user to replace an appointment.
    /// - Parameter appointmentId: The appointment identifier of the current appointment. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync or ShowReplaceAppointmentAsync call.
    /// - Parameter appointment: The object representing the information for the appointment to replace the current appointment.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Replace Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Replace Appointment UI displays around the button, not overlapping it.
    /// - Parameter preferredPlacement: The Placement that describes the preferred placement of the Replace Appointment UI.
    /// - Returns: When this method completes, it returns a **String** object that represents the appointment. This serves as an appointment identifier for future reference when updating or removing. If the appointment identifier returned is an empty string, the appointment was not replaced in the Appointments provider app.
    public func showReplaceAppointmentAsync(_ appointmentId: Swift.String, _ appointment: WindowsApplicationModelAppointments_Appointment?, _ selection: WindowsFoundation_Rect, _ preferredPlacement: WindowsUIPopups_Placement) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showReplaceAppointmentWithPlacementAsync(appointmentId, appointment, selection, preferredPlacement))
    }

    /// Shows the Appointments provider Replace Appointment UI, to enable the user to replace an appointment.
    /// - Parameter appointmentId: The appointment identifier of the current appointment. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync or ShowReplaceAppointmentAsync call.
    /// - Parameter appointment: The object representing the information for the appointment to replace the current appointment.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Replace Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Replace Appointment UI displays around the button, not overlapping it.
    /// - Parameter preferredPlacement: The Placement that describes the preferred placement of the Replace Appointment UI.
    /// - Parameter instanceStartDate: The start date and time of the appointment instance to replace.
    /// - Returns: When this method completes, it returns a **String** object that represents the appointment. This serves as an appointment identifier for future reference when updating or removing. If the appointment identifier returned is an empty string, the appointment was not replaced in the Appointments provider app.
    public func showReplaceAppointmentAsync(_ appointmentId: Swift.String, _ appointment: WindowsApplicationModelAppointments_Appointment?, _ selection: WindowsFoundation_Rect, _ preferredPlacement: WindowsUIPopups_Placement, _ instanceStartDate: WindowsFoundation_DateTime) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showReplaceAppointmentWithPlacementAndDateAsync(appointmentId, appointment, selection, preferredPlacement, instanceStartDate))
    }

    /// Shows the Appointments provider Remove Appointment UI, to enable the user to remove an appointment.
    /// - Parameter appointmentId: The appointment identifier. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync call.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Remove Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Remove Appointment UI displays around the button, not overlapping it.
    /// - Returns: When this method completes, it returns a **Boolean** value that indicates whether the Appointment provider app removed the appointment.
    public func showRemoveAppointmentAsync(_ appointmentId: Swift.String, _ selection: WindowsFoundation_Rect) throws -> WindowsFoundation_IAsyncOperation<Swift.Bool> {
        try COM.NullResult.unwrap(_interop.showRemoveAppointmentAsync(appointmentId, selection))
    }

    /// Shows the Appointments provider Remove Appointment UI, to enable the user to remove an appointment.
    /// - Parameter appointmentId: The appointment identifier. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync call.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Remove Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Remove Appointment UI displays around the button, not overlapping it.
    /// - Parameter preferredPlacement: The Placement that describes the preferred placement of the Remove Appointment UI.
    /// - Returns: When this method completes, it returns a **Boolean** value that indicates whether the Appointment provider app removed the appointment.
    public func showRemoveAppointmentAsync(_ appointmentId: Swift.String, _ selection: WindowsFoundation_Rect, _ preferredPlacement: WindowsUIPopups_Placement) throws -> WindowsFoundation_IAsyncOperation<Swift.Bool> {
        try COM.NullResult.unwrap(_interop.showRemoveAppointmentWithPlacementAsync(appointmentId, selection, preferredPlacement))
    }

    /// Shows the Appointments provider Remove Appointment UI, to enable the user to remove an appointment.
    /// - Parameter appointmentId: The appointment identifier. This is typically obtained from the async return value of a previous ShowAddAppointmentAsync call.
    /// - Parameter selection: The Rect is the rectangular area of user selection (for example, pressing a button), around which the operating system displays the Remove Appointment UI, not within that rectangular area. For example, if an app uses a button to show the Rect, pass the Rect of the button so the Remove Appointment UI displays around the button, not overlapping it.
    /// - Parameter preferredPlacement: The Placement that describes the preferred placement of the Remove Appointment UI.
    /// - Parameter instanceStartDate: The start date and time of the appointment instance to remove.
    /// - Returns: When this method completes, it returns a **Boolean** value that indicates whether the Appointment provider app removed the appointment.
    public func showRemoveAppointmentAsync(_ appointmentId: Swift.String, _ selection: WindowsFoundation_Rect, _ preferredPlacement: WindowsUIPopups_Placement, _ instanceStartDate: WindowsFoundation_DateTime) throws -> WindowsFoundation_IAsyncOperation<Swift.Bool> {
        try COM.NullResult.unwrap(_interop.showRemoveAppointmentWithPlacementAndDateAsync(appointmentId, selection, preferredPlacement, instanceStartDate))
    }

    /// Shows the Appointments provider app's primary UI. This typically displays a time frame from an appointments calendar.
    /// - Parameter timeToShow: A date and time object that specifies the beginning of the time frame that the Appointments provider app should display.
    /// - Parameter duration: A timespan that hints to the Appointments provider app how long the time frame shown should be.
    /// - Returns: When this method returns, it does not return a result. On completion, the AsyncActionCompletedHandler  specified by get_Completed / Completed is invoked.
    public func showTimeFrameAsync(_ timeToShow: WindowsFoundation_DateTime, _ duration: WindowsFoundation_TimeSpan) throws -> WindowsFoundation_IAsyncAction {
        try COM.NullResult.unwrap(_interop.showTimeFrameAsync(timeToShow, duration))
    }

    /// Shows the Appointments provider Appointment Details UI, to enable the user to view the specified appointment.
    /// - Parameter appointmentId: The LocalId of the appointment to be displayed.
    /// - Returns: When this method returns, it does not return a result. On completion, the AsyncActionCompletedHandler  specified by get_Completed / Completed is invoked.
    public func showAppointmentDetailsAsync(_ appointmentId: Swift.String) throws -> WindowsFoundation_IAsyncAction {
        try COM.NullResult.unwrap(_interop.showAppointmentDetailsAsync(appointmentId))
    }

    /// Shows the Appointments provider Appointment Details UI, to enable the user to view the specified appointment.
    /// - Parameter appointmentId: The LocalId of the appointment to be displayed.
    /// - Parameter instanceStartDate: A DateTime object with the start time of the appointment instance to be displayed.
    /// - Returns: When this method returns, it does not return a result. On completion, the AsyncActionCompletedHandler  specified by get_Completed / Completed is invoked.
    public func showAppointmentDetailsAsync(_ appointmentId: Swift.String, _ instanceStartDate: WindowsFoundation_DateTime) throws -> WindowsFoundation_IAsyncAction {
        try COM.NullResult.unwrap(_interop.showAppointmentDetailsWithDateAsync(appointmentId, instanceStartDate))
    }

    /// Shows the Appointments provider Add Appointment UI including the full edit experience, to enable the user to add an appointment.
    /// - Parameter appointment: The new appointment to be added.
    /// - Returns: An asynchronous operation that returns a string containing an appointment ID upon successful completion.
    public func showEditNewAppointmentAsync(_ appointment: WindowsApplicationModelAppointments_Appointment?) throws -> WindowsFoundation_IAsyncOperation<Swift.String> {
        try COM.NullResult.unwrap(_interop.showEditNewAppointmentAsync(appointment))
    }

    /// Retrieves an AppointmentStore object that enables searching or retrieving appointments on the device.
    /// - Parameter options: Specifies the type of read/write access requested.
    /// - Returns: An asynchronous operation that returns an AppointmentStore object upon successful completion.
    public func requestStoreAsync(_ options: WindowsApplicationModelAppointments_AppointmentStoreAccessType) throws -> WindowsFoundation_IAsyncOperation<WindowsApplicationModelAppointments_AppointmentStore?> {
        try COM.NullResult.unwrap(_interop.requestStoreAsync(options))
    }

    // MARK: Implementation details
}