// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Provides a method to delay the display of the **Pin to Start**  flyout, as well as methods through which you can set the visual elements of the secondary tile to be presented in that flyout as well as alternate versions of the tile that can also be presented as options.
public final class WindowsUIStartScreen_VisualElementsRequest: WindowsRuntime.WinRTImport<WindowsUIStartScreen_VisualElementsRequestProjection> {
    // MARK: Windows.UI.StartScreen.IVisualElementsRequest members

    /// Gets a set of objects that provide alternate logo images, background and foreground colors, and display name. These alternates are shown to the user in the **Pin to Start**  flyout.
    /// - Returns: An array of SecondaryTileVisualElements objects, each of which provides the information for an alternate presentation of the secondary tile.
    public var alternateVisualElements: WindowsFoundationCollections_IVectorView<WindowsUIStartScreen_SecondaryTileVisualElements?> {
        get throws {
            try COM.NullResult.unwrap(_interop.get_AlternateVisualElements())
        }
    }

    /// Gets the approximate time at which the deferral will time-out.
    /// - Returns: The time at which the deferral will time out.
    public var deadline: WindowsFoundation_DateTime {
        get throws {
            try _interop.get_Deadline()
        }
    }

    /// Gets a copy of the parent secondary tile's SecondaryTileVisualElements object. The visual elements properties can be set or read through this object. When deferral is complete (or the handler returns without a deferral), the properties in this object are verified and then reflected in the **Pin to Start**  flyout.
    /// - Returns: The object through which you'll access the visual element properties.
    public var visualElements: WindowsUIStartScreen_SecondaryTileVisualElements {
        get throws {
            try COM.NullResult.unwrap(_interop.get_VisualElements())
        }
    }

    /// Retrieves a deferral object, which allows the app time to provide information and assets used in the **Pin to Start**  flyout.
    /// - Returns: The deferral object.
    public func getDeferral() throws -> WindowsUIStartScreen_VisualElementsRequestDeferral {
        try COM.NullResult.unwrap(_interop.getDeferral())
    }

    // MARK: Implementation details
}