// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Provides functionality for controlling the torch LED settings on a capture device.
public final class WindowsMediaDevices_TorchControl: WindowsRuntime.WinRTImport<WindowsMediaDevices_TorchControlProjection> {
    // MARK: Windows.Media.Devices.ITorchControl members

    /// Gets or sets a value that enables and disables the torch LED on the device.
    /// - Returns: **true** if the torch LED is enabled; otherwise, **false**.
    public var enabled: Swift.Bool {
        get throws {
            try _interop.get_Enabled()
        }
    }

    /// Gets or sets a value that enables and disables the torch LED on the device.
    /// - Returns: **true** if the torch LED is enabled; otherwise, **false**.
    public func enabled(_ value: Swift.Bool) throws {
        try _interop.put_Enabled(value)
    }

    /// Gets or sets the intensity of the torch LED.
    /// - Returns: The power percent the torch LED is set to.
    public var powerPercent: Swift.Float {
        get throws {
            try _interop.get_PowerPercent()
        }
    }

    /// Gets or sets the intensity of the torch LED.
    /// - Returns: The power percent the torch LED is set to.
    public func powerPercent(_ value: Swift.Float) throws {
        try _interop.put_PowerPercent(value)
    }

    /// Gets a value that specifics if the device allows the torch LED power settings to be changed.
    /// - Returns: **true** if the power settings can be modified; otherwise, **false**.
    public var powerSupported: Swift.Bool {
        get throws {
            try _interop.get_PowerSupported()
        }
    }

    /// Gets a value that specifies if the capture device supports the torch control.
    /// - Returns: **true** if the capture device supports the torch control; otherwise, **false**.
    public var supported: Swift.Bool {
        get throws {
            try _interop.get_Supported()
        }
    }

    // MARK: Implementation details
}