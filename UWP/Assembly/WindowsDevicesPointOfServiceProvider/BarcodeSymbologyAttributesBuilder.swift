// Generated by swift-winrt
// swiftlint:disable all

import CWinRT
import WindowsRuntime
import UWP
import struct Foundation.UUID

/// Represents the builder of barcode symbology attributes.
public final class WindowsDevicesPointOfServiceProvider_BarcodeSymbologyAttributesBuilder: WindowsRuntime.WinRTImport<WindowsDevicesPointOfServiceProvider_BarcodeSymbologyAttributesBuilderProjection> {
    // MARK: IActivationFactory members

    public convenience init() throws {
        self.init(_transferringRef: try Self._iactivationFactory.activateInstance(projection: WindowsDevicesPointOfServiceProvider_BarcodeSymbologyAttributesBuilderProjection.self))
    }

    // MARK: Windows.Devices.PointOfService.Provider.IBarcodeSymbologyAttributesBuilder members

    /// Gets or sets whether the barcode symbology supports transmitting the check digit to the client as part of the raw data.
    /// - Returns: True if the barcode symbology supports transmitting the check digit as raw data; otherwise, false.
    public var isCheckDigitTransmissionSupported: Swift.Bool {
        get throws {
            try _interop.get_IsCheckDigitTransmissionSupported()
        }
    }

    /// Gets or sets whether the barcode symbology supports transmitting the check digit to the client as part of the raw data.
    /// - Returns: True if the barcode symbology supports transmitting the check digit as raw data; otherwise, false.
    public func isCheckDigitTransmissionSupported(_ value: Swift.Bool) throws {
        try _interop.put_IsCheckDigitTransmissionSupported(value)
    }

    /// Gets or sets whether the barcode symbology supports check digit validation.
    /// - Returns: True if the barcode symbology supports check digit validation; otherwise, false.
    public var isCheckDigitValidationSupported: Swift.Bool {
        get throws {
            try _interop.get_IsCheckDigitValidationSupported()
        }
    }

    /// Gets or sets whether the barcode symbology supports check digit validation.
    /// - Returns: True if the barcode symbology supports check digit validation; otherwise, false.
    public func isCheckDigitValidationSupported(_ value: Swift.Bool) throws {
        try _interop.put_IsCheckDigitValidationSupported(value)
    }

    /// Gets or sets whether the barcode symbology supports multiple decode lengths.
    /// - Returns: True if the barcode symbology supports multiple decode lengths; otherwise, false.
    public var isDecodeLengthSupported: Swift.Bool {
        get throws {
            try _interop.get_IsDecodeLengthSupported()
        }
    }

    /// Gets or sets whether the barcode symbology supports multiple decode lengths.
    /// - Returns: True if the barcode symbology supports multiple decode lengths; otherwise, false.
    public func isDecodeLengthSupported(_ value: Swift.Bool) throws {
        try _interop.put_IsDecodeLengthSupported(value)
    }

    /// Creates a BarcodeSymbologyAttributes object from the properties of this class.
    /// - Returns: The symbology attributes.
    public func createAttributes() throws -> WindowsDevicesPointOfService_BarcodeSymbologyAttributes {
        try COM.NullResult.unwrap(_interop.createAttributes())
    }

    // MARK: Implementation details

    private static var _iactivationFactory_storage: COM.COMInterop<CWinRT.SWRT_IActivationFactory>? = nil

    internal static var _iactivationFactory: COM.COMInterop<CWinRT.SWRT_IActivationFactory> {
        get throws {
            try _iactivationFactory_storage.lazyInit {
                try WindowsRuntime.getActivationFactoryPointer(activatableId: "Windows.Devices.PointOfService.Provider.BarcodeSymbologyAttributesBuilder", id: CWinRT.SWRT_IActivationFactory.iid)
            }
        }
    }
}